#this file exists solely for testing new functions developed elsewhere and to see how theyll fit into the current system.
import interface
import Serialsetup

# interface.make()

# N_samples = int(interface.N_frames) 
# domain = interface.domain
# attenuation = interface.attenuation
# coupling = interface.coupling
# num_channels = interface.num_channels
# print(num_channels)


# print(Serialsetup.convertdata(attenuation,coupling,num_channels))
# print(Serialsetup.makecheckbyte(num_channels))
